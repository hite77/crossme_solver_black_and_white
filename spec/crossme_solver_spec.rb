require 'crossme_solver'

RSpec.describe Crossme_solver, "#foo" do
  context "foo" do
    it "does something" do
      solver = Crossme_solver.new
      expect(solver.method).to eq 80
    end
  end
end
